"""Self-contained simulation engines for the HF Space.

Mirrors the relevant CPU-only subset of `dcrisk` (the full package lives at
github.com/alidenewade/actuarial-pricing-data-centers) so the Space ships
without external installs beyond requirements.txt.
"""
